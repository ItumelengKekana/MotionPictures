@model IEnumerable<MovingPicturesV2.Models.MediaTypeModel>

<div class="container p-3">
	<div class="row pt-4">
		<div class="col-6">
			<h2 class="text-primary">Media Type List</h2>
		</div>
		<div class="col-6 text-end">
			<a class="btn btn-primary" asp-controller="MediaType" asp-action="Create"><i class="bi bi-plus-circle"></i>&nbsp; Create New Media Type</a>
		</div>
	</div>
	<br />
	<br />
	<table class="table table-bordered table-striped" style="width:100%">
		<thead>
			<tr>
				<th>
					Media Type
				</th>

			</tr>
		</thead>
		<tbody>
			@foreach (var obj in Model)
			{
				<tr>
					<td width="50%">
						@obj.Name
					</td>
					<td>
						<div class="w-75 btn-group" role="group">
							<a class="btn btn-primary mx-2" asp-controller="MediaType" asp-action="Edit" asp-route-id="@obj.Id"><i class="bi bi-pencil-square"></i>Edit</a>
						</div>
					</td>
					<td>
						<div class="w-75 btn-group" role="group">
							<a class="btn btn-danger mx-2" asp-controller="MediaType" asp-action="Delete" asp-route-id="@obj.Id"><i class="bi bi-trash"></i>Delete</a>
						</div>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>
